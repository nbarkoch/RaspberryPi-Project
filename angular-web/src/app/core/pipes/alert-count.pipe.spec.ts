import { AlertCountPipe } from './alert-count.pipe';

describe('AlertCountPipe', () => {
  it('create an instance', () => {
    const pipe = new AlertCountPipe();
    expect(pipe).toBeTruthy();
  });
});
